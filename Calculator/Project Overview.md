
Idea is to build a calculator, using it as a opportunity to get a first real project in C++. It should easily handle arithmetic but also functions (trig, log, powers)

	Dealing with parsing, tokenization, operator precedence, floating point accurace and numerical stability

Since I want to add graphing and later on build my own GUI, this project will be as complex as I see fit.


Goals:
- Implement arithmetic and basic functions
- Build a recursive descent parser
- Ensure numerical stability

Non‑Goals:
- GUI implementation (for now)
- Networking or external libraries
